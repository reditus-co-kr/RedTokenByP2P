/*
import { Artifacts } from '../util/artifacts';
const {
  RedTokenCore,
  RedTokenCoreTest,
  RedTokenOwnership,
  RedTokenBase,
  RedTokenAccessControl,
  ERC721,
  SafeMath,
  Address
} = new Artifacts(artifacts);

module.exports = (deployer: any, network: string) => {
  const RedTokenContract = network === 'test' ? RedTokenCoreTest : RedTokenCore;
  deployer.deploy(RedTokenContract);
};
*/